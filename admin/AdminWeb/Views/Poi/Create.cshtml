@model POI
@{
    ViewData["Title"] = "Tạo POI";
    ViewData["ActionName"] = "Create";
}

<div class="d-flex align-items-end justify-content-between flex-wrap gap-3 mb-4">
    <div>
        <div class="breadcrumbs-inline mb-2">
            <a asp-controller="Home" asp-action="Index">Dashboard</a>
            <span>/</span>
            <a asp-action="Index">POI</a>
            <span>/</span>
            <span>Tạo mới</span>
        </div>
        <h1 class="page-title">Tạo POI mới</h1>
        <p class="page-subtitle">Nhập dữ liệu địa điểm để mobile map hiển thị tên, mô tả và vùng phủ.</p>
    </div>
</div>

<partial name="_Form" model="Model" />

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
